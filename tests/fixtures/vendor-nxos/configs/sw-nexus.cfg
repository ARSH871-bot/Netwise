! Netwise test fixture -- Cisco NX-OS.
!
! DELIBERATE FAULT: the interface applies an access-group named FINANCE_IN
! that is never defined anywhere in this config. On a real device the rule
! silently does nothing. undefinedReferences finds this.
!
hostname sw-nexus
!
ip access-list SERVER_IN
  10 permit tcp any any eq 443
  20 deny ip any any
!
interface Ethernet1/1
  description finance segment
  no switchport
  ip address 10.40.0.1/24
  ip access-group FINANCE_IN in
!