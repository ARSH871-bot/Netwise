!
! Netwise test fixture -- SYNTHETIC, GENERATED. Not a real network.
!
! Built by tools/make_scale_fixture.py. Do not edit by hand: rebuild it.
!
! One of 10 devices in this snapshot. Every device is the same router
! with its own name and LAN. There is NO routing between them and no device
! references another, so this exercises snapshot SIZE and policy COVERAGE,
! and says nothing about topology.
!
! NOT covered by any built-in rule -- PC-049 should name it.
!
hostname rtr-dev009
!
interface GigabitEthernet0/0
 ip address 10.19.10.1 255.255.255.0
 ip access-group acl_in in
!
ip access-list extended acl_in
 permit udp 10.19.10.0 0.0.0.255 host 218.8.104.58 eq domain
 permit tcp 10.19.10.0 0.0.0.255 host 10.20.0.5 eq 443
 deny   ip any any
!
